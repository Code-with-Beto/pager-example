export type Chat = {
  id: string;
  prompt: string;
  response: string;
  title: string;
};

export type ColorPalette = {
  accent: string;
  accentText: string;
  menuBackground: string;
  menuSelected: string;
  muted: string;
  separator: string;
  surfaceBackground: string;
  surfaceBorder: string;
  text: string;
  userBubble: string;
};
